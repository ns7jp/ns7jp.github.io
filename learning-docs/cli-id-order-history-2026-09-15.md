# 順序検査を実際のHTTPログへ適用

実施日：2026年9月15日（JST）

本人がWSL Ubuntuでcheck-run-ids-ordered.shを、以前のHTTP確認で保存した実行ID付きhistory.logへ適用しました。AIが手順を案内し、本人提示の端末画像を読み取って公開用要約を作成しています。AIの代理実行やCIの実測ではありません。

[人工サンプルでの順序検査](./cli-id-order-check-2026-09-15.md)に続き、[本人が保存したHTTP実行ログ](./cli-http-run-id-2026-09-15.md)へ適用した追加演習です。

## 本人が確認した結果

| 段階 | 操作 | 提示画面で確認した結果 |
| --- | --- | --- |
| 対象確認 | findで順序検査スクリプトと実行ログを検索 | 各1件、両検索の終了コード0 |
| 検査 | bashでスクリプトにログを指定 | 2つのIDが各START=1 END=1、順序不正の表示なし |
| 終了コード確認 | 実行直後に結果を表示 | 順序検査の終了コード0 |
| 記録保存 | 本人が要約を保存し、catで再表示 | 保存終了コード0と記録内容を確認 |

実行の形は次のとおりです。変数は事前に確認した各1件のファイルを指します。

```bash
bash "$ordered_checker" "$id_log"
echo "順序検査の終了コード=$?"
```

実IDは公開しません。今回、ファイル内の行順について、検査対象の各IDのSTARTがENDより先にあることと、各件数が1であることを確認しました。実イベントの発生順を別の証拠で検証したものではありません。

## 判定の意味と理解確認

対象ログは過去のHTTP 200と意図したHTTP 404を含みます。検査0は記録の件数・行順の合格であり、HTTP確認の全件成功や現在のnginxの状態を示しません。このスクリプトはENDのcodeの形式・値を判定していません。

「順序検査が0でもHTTP確認がすべて成功したと言えない理由」に、本人は「対象ログには以前のHTTP 200と404の両方が含まれているから」と回答しました。AIは順序検査とHTTP成否の違いを補足しました。支援なしの独立再実施は未確認です。

本人がid-order-history-2026-09-15.txtを保存し、保存終了コード0と内容の再表示を提示しました。

## 証拠の出所と未実施範囲

- 根拠は本人提示画像です。個人環境のIP・ユーザー名・ホスト名・絶対パス、実ID、鍵情報、画像、ローカル原本、完全な生ログは公開しません。
- **NOT RUN**：ENDのcodeの形式・値の検査、実際の同時実行、ログ全体の完全性検証。
- 今回は保存済みログの読み取りで、新しいHTTP接続や既存ログ・スクリプトの変更はありません。演習要約を新規保存しました。
- 第三者の独立再検証や原本のハッシュ照合は未実施です。静的チェックやCIは文書の検証であり、本人環境の順序検査の再実行ではありません。

今回の到達点は、順序検査付きスクリプトを実際の保存済みログに適用し、2つのIDの開始・終了件数と行順を確認して記録できたことです。
