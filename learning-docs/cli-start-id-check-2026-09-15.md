# STARTの欠落・重複を検査

実施日：2026年9月15日（JST）

本人がWSL Ubuntuで、[実行IDごとの件数検査スクリプト](./cli-run-id-check-2026-09-15.md)に人工サンプルを渡し、STARTの欠落と重複を検出できるか確認しました。AIが手順を案内し、本人提示の端末画像を読み取って公開用要約を作成しています。AIの代理実行やCIの実測ではありません。

前回はEND欠落・重複を検査し、今回はSTART側を追加確認しました。check-run-ids.sh自体は変更していません。

## 本人が実施した操作と結果

検査スクリプトを1件検索し、新しい作業フォルダを作成しました。検索・作業先作成は終了コード0でした。次の2種類をprintfで作成し、bash経由で検査しました。

### START欠落

```text
END run_id=A code=0
```

サンプル保存は終了コード0。検査表示はID=A START=0 END=1で、ID検査の終了コードは1でした。

### START重複

```text
START run_id=A
START run_id=A
END run_id=A code=0
```

サンプル保存は終了コード0。検査表示はID=A START=2 END=1で、ID検査の終了コードは1でした。

## 結果の意味と理解確認

両入力ともENDにcode=0を書いていますが、検査スクリプトはSTARTがちょうど1件ではないことを検出しました。code=0は人工入力として書いた値で、HTTP成功や実際の処理実行を示すものではありません。検査結果1と、ログ中のcode=0を区別します。

「ENDにcode=0があってもSTARTが2件なら対応に問題なしと判断してよいか」という質問に、本人は「ダメです」と回答しました。AIは処理結果と記録の対応の違いを補足しました。支援なしの独立再実施は未確認です。

本人がstart-id-check-2026-09-15.txtをWSL内に保存し、保存終了コード0とcatによる内容の再表示を提示しました。

## 証拠の出所と未実施範囲

- 根拠は本人提示画像です。人工入力の作成・検査の本人実施であり、新しいHTTP接続や実際の同時実行の試験ではありません。
- **NOT RUN**：空ログ、不正ID、読み取りエラー、実際の同時実行の試験。
- 検査はIDごとの件数です。START・ENDの順序、ログ全体の完全性、ENDのcodeの形式・値やHTTP成功を検証する処理ではありません。
- 既存ログ・nginx設定・サービスは変更せず、検証用フォルダ・人工サンプル・演習記録を作成しました。
- 個人環境のIP・ユーザー名・ホスト名・絶対パス、鍵情報、画像、ローカル原本、完全な生ログは公開しません。
- 第三者の独立再検証や原本のハッシュ照合は未実施です。静的チェックやCIは文書の検証であり、本人環境の試験の再実行ではありません。

今回の到達点は、STARTが0件または2件でENDが1件の人工サンプルを検査し、いずれも件数の不整合として終了コード1を返すことを確認・記録したことです。
