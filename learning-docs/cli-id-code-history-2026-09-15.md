# 終了コード検査付き版を実ログへ適用

実施日：2026年9月15日（JST）

本人がWSL Ubuntuでcheck-run-ids-code.shを、以前のHTTP確認で保存した実行ID付きhistory.logへ適用しました。AIが手順を案内し、本人提示の端末画像を読み取って公開用要約を作成しています。AIの代理実行やCIの実測ではありません。

[END行の終了コード検査](./cli-id-end-code-check-2026-09-15.md)を、[本人が保存したHTTP実行ログ](./cli-http-run-id-2026-09-15.md)に適用した追加演習です。今回、新しいHTTP接続やログ変更は行っていません。

## 本人が確認した結果

| 段階 | 操作 | 提示画面で確認した結果 |
| --- | --- | --- |
| 対象確認 | findで修正版と実行ログを検索 | 各1件、両検索の終了コード0 |
| 検査 | bashで修正版にログを指定 | 2つのIDが各START=1 END=1、不正の表示なし |
| 結果確認 | 実行直後に終了コードを表示 | 検査スクリプトの終了コード0 |
| 記録保存 | 本人が要約を保存し、catで再表示 | 保存終了コード0と内容を確認 |

実行の形は次のとおりです。変数は事前に確認した各1件のファイルを指します。

```bash
bash "$code_checker" "$id_log"
echo "終了コード検査付き版の終了コード=$?"
```

今回の実IDは公開しません。保存済みログに記録された正常結果code=0と、意図したHTTP 404によるcode=22は、ともに終了コードの形式と範囲の検査を通りました。今回の画面は検査結果であり、元のHTTP試験を再実行したものではありません。

## 検査合格とHTTP成功を区別する

このスクリプトはIDごとのSTART・END件数、行順、ENDのcode形式・0〜255の範囲を検査します。記録された処理の成功を要求する検査ではないため、code=22が含まれていても記録が検査条件を満たせば0を返します。

本人は最初、この違いが分からないと回答しました。AIの説明後に「この検査は処理が成功したかではなく、結果が正しく記録されているかを調べるから」と説明しました。支援なしの独立再実施は未確認です。

本人がid-code-history-2026-09-15.txtをWSL内に保存し、保存終了コード0と記録内容の再表示を提示しました。

## 証拠の出所と未実施範囲

- 根拠は本人提示画像です。個人環境のIP・ユーザー名・ホスト名・絶対パス、実ID、鍵情報、画像、ローカル原本、完全な生ログは公開しません。
- **NOT RUN**：実際の同時実行、ログ全体の完全性検証、定期検査。
- 検査条件に合格したことを、すべての不正入力・記録欠落・改ざんを検出できる保証とはしません。
- 既存スクリプトとログは変更せず、保存済みログの読み取りと演習要約の保存を行いました。現在のnginx状態はこの検査では確認していません。
- 第三者の独立再検証や原本のハッシュ照合は未実施です。静的チェックやCIは文書の検証であり、本人環境の検査の再実行ではありません。

今回の到達点は、終了コード検査付き修正版が保存済みの2実行の記録を検査でき、HTTP成否と記録の検査合格を区別して確認・記録したことです。
