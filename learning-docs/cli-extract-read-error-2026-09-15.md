# 抽出スクリプトの読み取りエラー確認

実施日：2026年9月15日（JST）

本人がWSL Ubuntuで、[extract-results.sh](./cli-id-extractor-script-2026-09-15.md)に存在しない入力ログを指定しました。検査処理はcheck-run-ids-code.shです。AIが手順を案内し、本人提示の端末画像を読み取って公開用要約を作成しています。AIの代理実行やCIの実測ではありません。

## 本人が確認した結果

| 段階 | 操作 | 提示画面で確認した結果 |
| --- | --- | --- |
| 準備 | 両スクリプトを検索、新しい作業先作成 | 各1件、両検索と作成の終了コード0 |
| 実行 | 未作成のnot-created.logを指定し、出力と検査メッセージを別ファイルへ保存 | 抽出スクリプトの終了コード2 |
| 出力確認 | wc -cでresults.txtを確認 | 0バイト、サイズ確認終了コード0 |
| 検査ログ確認 | validation.logをcatで表示 | ファイル不在、検査不能、抽出しない旨を表示。cat終了コード0 |
| 記録保存 | 本人が要約を保存し、catで再表示 | 保存終了コード0と記録内容を確認 |

ログにはNo such file or directory、検査不能：ログを開けません、検査不合格のため抽出しません：終了コード=2が残りました。検査のファイルを開く失敗が、抽出スクリプトの終了コード2として呼び出し元へ返ることを確認しました。

## 結果ファイルが空であることの意味

今回はシェルのリダイレクトで結果ファイルを作成しましたが、入力を読めず抽出しなかったため0バイトでした。「抽出が正常完了して0件だった」という意味ではありません。サイズ確認・catの0と、抽出処理の2は別の結果です。

今回試したのは入力ログの不在であり、出力先エラーではありません。既存ログの削除や新しいHTTP接続は行っていません。

## 理解確認と保存

結果ファイルが0バイトという理由だけで正常抽出・0件と判断してよいか、という質問に本人は「ダメです」と回答しました。AIは終了コードと検査ログも必要であると補足しました。支援なしの独立再実施は未確認です。

本人がextract-read-error-2026-09-15.txtをWSL内に保存し、保存終了コード0と内容の再表示を提示しました。

## 証拠の出所と未実施範囲

- 根拠は本人提示画像です。個人環境のIP・ユーザー名・ホスト名・絶対パス、鍵情報、画像、ローカル原本、完全な生ログは公開しません。
- **NOT RUN**：権限不足・読み取り途中のエラー・出力先エラーの試験、定期実行。
- 検査と抽出で別々にファイルを読む制約や、検査後に内容が変わる問題への未対処は残っています。
- スクリプト自体は変更せず、作業フォルダ・出力ファイル・演習記録を作成しました。nginx設定変更・サービス停止は行っていません。
- 第三者の独立再検証や原本のハッシュ照合は未実施です。静的チェックやCIは文書の検証であり、本人環境での読み取りエラー試験の再実行ではありません。

今回の到達点は、入力を開けない場合に抽出結果を出さず終了コード2を返し、空ファイルだけでは成功と判断できないことを確認して記録したことです。
