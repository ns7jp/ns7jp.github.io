# HTTP実行用スクリプトのログ保存先エラー検出

実施日：2026年9月14日（JST）

本人がWSL Ubuntuでrun-http-check.shに存在しないサブフォルダ内のログ保存先を指定し、HTTP確認を開始する前に終了コード125で停止することを確認しました。保存先を存在するフォルダ内へ変更すると、HTTP確認とログ保存が成功しました。AIが手順を案内し、本人が提示した端末画像を読み取って公開用要約を作成しています。AIの代理実行やCIの実測ではありません。

[HTTP確認とログ保存のスクリプト化](./cli-http-runner-2026-09-14.md)で未試験だった125の経路のうち、ログを開けない場合の追加検証です。

## 本人が実施した操作と結果

| 段階 | 操作 | 提示画面で確認した結果 |
| --- | --- | --- |
| 準備 | 実行用・本文確認スクリプトを検索、新規検証フォルダ作成 | 各1件を表示、両検索・フォルダ作成の終了コード0 |
| 異常条件 | 存在しないnot-createdサブフォルダ内のhistory.logを保存先に指定し、bash -xで実行 | ログを開くexecの段階でNo such file or directory、exit 125 |
| 結果確認 | 実行直後の終了コードを表示 | 実行用スクリプトの終了コード125 |
| 保存先修正 | 存在する検証フォルダ直下のhistory.logへ引数を変更 | 実行用スクリプトの終了コード0 |
| 応答・保存確認 | 作成されたログをcatで再表示 | 開始日時、HTTP 200、Welcome to nginx!を含む本文の行、成功メッセージ、確認処理の終了コード0。catも終了コード0 |
| 演習記録保存 | 本人が要約を保存し、catで再表示 | 保存終了コード0と記録内容を確認 |

最初の試験では、トレースが引数・確認スクリプトの存在と可読性の確認、ログを開くexec、exit 125の順に進みました。本文確認スクリプトを呼び出すbashには到達していません。この判断はコードと実行トレースに基づき、パケットキャプチャで通信不在を検証したものではありません。

正常な再実行では本文中の指定文字列を確認しました。これは文字列の包含判定であり、本文全体の完全一致ではありません。

## エラーの段階を区別する

今回失敗した箇所は、HTTP接続の前のログ準備です。125を「Webサイトへの接続失敗」と報告せず、ログ保存先の問題として扱いました。

原因は意図的に指定した親フォルダの不在です。既存ログを削除したり、nginxを停止したりして再現したわけではありません。スクリプト自体は変更せず、保存先の引数だけを変更して再実行しました。

125はこの実行用スクリプトが準備などのエラー用に定義した値です。すべての125が同じ原因だという意味ではなく、他の引数・準備エラーの経路は今回未試験です。

## 理解確認と保存

「ログを開けず125で止まった結果をWebサイトへの接続失敗と報告してよいか」という質問に本人は「ダメです」と回答しました。AIはHTTP確認前の停止であることを補足しました。支援なしの独立再実施は未確認です。

本人がWSL内にhttp-runner-save-error-2026-09-14.txtを保存し、保存終了コード0と内容の再表示を提示しました。

## 証拠の出所と未実施範囲

- 根拠は会話内の本人提示画像です。個人環境のIP・ユーザー名・ホスト名・絶対パス、鍵情報、画像、ローカル原本、完全な生ログは公開しません。
- **NOT RUN**：権限不足・容量不足・実行途中の書き込み失敗、他の125の経路、定期実行、自動通知。
- **未確認**：今回のアクセスログ、一時ファイル削除結果、第三者の独立再検証。
- 今回のログ準備失敗検出だけで、あらゆるログ保存エラーへの耐性を保証しません。
- 検証用フォルダ、正常再実行時のログ、演習要約を作成しました。nginx設定変更・サービス停止は行っていません。
- 静的チェックやCIは文書の検証であり、本人環境でのエラー試験やHTTP取得の再実行ではありません。

今回の到達点は、保存先を開けない場合にHTTP確認前で125を返し、適切な保存先ではHTTP 200・本文一致とログ保存が成功することを区別して確認したことです。
