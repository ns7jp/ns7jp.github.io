# Trivy: Lab で承認済みの設定検出を抑制する。
# 本番では .trivyignore に追加せず、別途承認プロセスを設けるのが望ましい。

# === Lab 用の妥協（README で明示済み） ===
# Ansible NOPASSWD:ALL → ansible/README.md §注意 で「本番では NOPASSWD 範囲を最小化」と明記
AVD-AVD-AN-0011
# Lab の Grafana admin / changeme → monitoring-stack/README.md §注意 で警告済
AVD-DKL-DS-002

# === 静的サイトの依存物がないため誤検出を抑制 ===
# scripts/check-*.js は Node の標準モジュールのみ使用、外部 npm パッケージなし
