# 読み取り対応版ID検査を実ログへ適用

実施日：2026年9月15日（JST）

本人がWSL Ubuntuでcheck-run-ids-readable.shを保存済みのHTTP実行ログへ適用しました。AIが手順を案内し、本人提示の端末画像を読み取って公開用要約を作成しています。AIの代理実行やCIの実測ではありません。

[読み取りエラーを区別する修正](./cli-id-read-check-2026-09-15.md)後、[実行IDを追加したHTTPログ](./cli-http-run-id-2026-09-15.md)を検査した追加演習です。今回、新しいHTTP接続は行っていません。

## 本人が確認した結果

| 段階 | 操作 | 提示画面で確認した結果 |
| --- | --- | --- |
| 対象検索 | findで修正版と実行ログを検索 | 各1件、両検索の終了コード0 |
| 実ログ検査 | bashで修正版へログを指定 | 2つのIDがそれぞれSTART=1 END=1 |
| 結果確認 | 実行直後の終了コード表示 | 修正版のID検査終了コード0 |
| 比較 | 元のID検査結果と比較 | 同じ2つのIDの件数一致 |
| 演習記録保存 | 本人が要約を保存し、catで再表示 | 保存終了コード0と内容を確認 |

実行の形は次のとおりです。各変数は検索で確認した1件のファイルを指します。

```bash
bash "$readable_checker" "$id_log"
echo "修正版のID検査終了コード=$?"
```

実IDは公開しません。元の検査結果は[IDごとの件数検査](./cli-run-id-check-2026-09-15.md)で確認した過去の結果であり、今回元のスクリプトも再実行したという意味ではありません。

## 結果の解釈と理解確認

検査0はIDごとのSTART・ENDが各1件あることを示します。対象ログには過去の正常HTTP 200と意図したHTTP 404が含まれますが、この検査はHTTPの全件成功を判定しません。

「ID検査が0ならHTTP確認もすべて成功したと言えるか」という質問に、本人は「言えない」と回答しました。AIは記録の件数確認とHTTP結果の成否の違いを補足しました。支援なしの独立再実施は未確認です。

本人がid-readable-history-2026-09-15.txtを保存し、保存終了コード0とcatによる内容の再表示を提示しました。既存ログとスクリプトは変更していません。

## 証拠の出所と未実施範囲

- 根拠は本人提示画像です。個人環境のIP・ユーザー名・ホスト名・絶対パス、実ID、鍵情報、画像、ローカル原本、完全な生ログは公開しません。
- **NOT RUN**：START・ENDの順序検査、ENDのcodeの形式・値の検査、実際の同時実行、読み取り途中のエラー試験。
- 今回は保存済みログの読み取りで、現在のnginxの状態や新しいHTTP応答を確認したものではありません。
- 検査の合格はログ全体の完全性や、開始終了以外の行の対応を保証しません。
- 演習要約を保存しました。nginx設定変更・サービス停止は行っていません。
- 第三者の独立再検証や原本のハッシュ照合は未実施です。静的チェックやCIは文書の検証であり、本人環境の検査の再実行ではありません。

今回の到達点は、ファイルを開けない場合を区別する修正版でも、保存済み実ログのID別件数を検査でき、元の結果と一致することを確認して記録したことです。
