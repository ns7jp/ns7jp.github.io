# 抽出結果の出力先エラー確認

実施日：2026年9月15日（JST）

本人がWSL Ubuntuで、[extract-results.sh](./cli-id-extractor-script-2026-09-15.md)の標準出力保存先に存在しないサブフォルダを指定し、呼び出し元のシェルが失敗することを確認しました。AIが手順を案内し、本人提示の端末画像を読み取って公開用要約を作成しています。AIの代理実行やCIの実測ではありません。

[前回の入力読み取りエラー](./cli-extract-read-error-2026-09-15.md)とは異なり、今回は入力に保存済みHTTP実行ログを指定し、出力先を開けない場合を試しました。

## 本人が実施した結果

| 段階 | 操作 | 提示画面で確認した結果 |
| --- | --- | --- |
| 準備 | 抽出・検査スクリプトと実行ログを検索、作業先作成 | 各1件、各検索・作成の終了コード0 |
| 出力先エラー | 未作成サブフォルダ内へ標準出力を指定 | 呼び出し元bashのNo such file or directory、呼び出し終了コード1 |
| 保存先修正 | 存在する検証フォルダ内へ出力先を変更 | 抽出スクリプトの終了コード0 |
| 出力確認 | 結果と検査ログをcatで表示 | IDとcode=0/22の2行、IDごとのSTART=1 END=1。両表示の終了コード0 |
| 記録保存 | 本人が演習要約を保存し再表示 | 保存終了コード0と内容を確認 |

初回は`bash -x "$extractor" "$code_checker" "$id_log" > "$output_test_dir/not-created/results.txt"`の形で実行しました。呼び出し元シェルの出力リダイレクトで失敗し、抽出スクリプトの内部トレースは表示されませんでした。出力先の準備はコマンド起動前に行われるため、抽出スクリプトを開始できなかった結果です。

正常再実行では、結果と検査メッセージを別ファイルへ保存しました。スクリプト・入力ログは変更せず、指定する出力先を直しました。新しいHTTP接続は行っていません。

## 終了コードの解釈と理解確認

初回の1はログ検査の不合格ではありません。返した処理は呼び出し元シェルであり、エラーは出力ファイルを開けないことでした。以前の不正入力で抽出スクリプトが返した1とは、同じ数字でも発生段階と意味が異なります。

「終了コード1だけで入力ログが不正だったと判断してよいか」という質問に、本人は「ダメです」と回答しました。AIは返した処理とエラーメッセージも確認する必要があると補足しました。支援なしの独立再実施は未確認です。

本人がextract-output-error-2026-09-15.txtを保存し、保存終了コード0とcatによる内容の再表示を提示しました。

## 証拠の出所と未実施範囲

- 根拠は本人提示画像です。個人環境のIP・ユーザー名・ホスト名・絶対パス、実ID、鍵情報、画像、ローカル原本、完全な生ログは公開しません。
- **NOT RUN**：出力先の権限不足・容量不足・途中の書き込み失敗の試験。
- 抽出処理が内部で出力先エラーを検出した試験ではなく、呼び出し元のシェルによる開始前のエラーを確認したものです。
- 既存ログとスクリプトは変更せず、作業フォルダ・正常再実行時の出力ファイル・演習記録を作成しました。nginx設定変更・サービス停止は行っていません。
- 第三者の独立再検証や原本のハッシュ照合は未実施です。静的チェックやCIは文書の検証であり、本人環境でのエラー試験・抽出の再実行ではありません。

今回の到達点は、出力先を開けずコマンド起動前に失敗する状態と、保存先修正後の抽出成功を確認し、終了コードを発生段階と合わせて解釈して記録したことです。
