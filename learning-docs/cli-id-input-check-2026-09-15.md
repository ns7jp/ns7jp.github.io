# ID検査の空ログ・不正ID確認

実施日：2026年9月15日（JST）

本人がWSL Ubuntuで、[check-run-ids.sh](./cli-run-id-check-2026-09-15.md)に空ログとIDが空の人工入力を渡しました。AIが手順を案内し、本人提示の端末画像を読み取って公開用要約を作成しています。AIの代理実行やCIの実測ではありません。

## 本人が実施した確認

検査スクリプトを1件検索し、新しい作業フォルダを作成しました。両操作の終了コードは0でした。スクリプト自体は変更していません。

| 入力 | 作成結果 | 検査の表示 | 検査終了コード |
| --- | --- | --- | --- |
| 内容のないempty.log | 作成終了コード0 | 判定保留：実行IDの記録なし | 3 |
| IDが空のinvalid-id.log | 保存終了コード0 | 形式不正：行1、形式不正：行2 | 1 |

不正IDのサンプルは次の2行です。

```text
START run_id=
END run_id= code=0
```

空ログでは検査対象の記録がありません。不正IDの入力ではSTARTとENDが存在していても、IDの形式を満たさないため不合格となりました。ENDのcode=0は人工入力であり、HTTP成功の実績ではありません。

## 判断・理解確認・保存

STARTとENDが1行ずつでもIDが空なら対応に問題なしと判断してよいか、という質問に本人は「ダメです」と回答しました。AIは、IDが空ではどの実行の開始と終了か識別できないと補足しました。

本人がid-input-check-2026-09-15.txtを保存し、保存終了コード0とcatによる内容の再表示を提示しました。支援なしの独立再実施は未確認です。

## 制約と未実施範囲

- 今回検証した不正形式はIDが空の場合だけです。他の不正ID形式や読み取りエラー、実際の同時実行はNOT RUNです。
- 終了コード3は検査対象のID記録がない場合の判定で、あらゆる入力で空ファイルを証明する値ではありません。
- この検査はSTART・ENDのID形式とIDごとの件数を調べます。順序、ENDのcodeの形式・値、ログ全体の完全性を検証する処理ではありません。
- 今回は人工入力の作成と検査で、新しいHTTP接続・既存ログ変更・nginx設定変更・サービス停止は行っていません。
- 根拠は本人提示画像です。個人環境のIP・ユーザー名・ホスト名・絶対パス、鍵情報、画像、ローカル原本、完全な生ログは公開しません。
- 第三者の独立再検証や原本のハッシュ照合は未実施です。静的チェックやCIは文書の検証であり、本人環境の検査の再実行ではありません。

今回の到達点は、空ログを判定保留の3、IDが空の記録を形式不正の1として区別し、正常扱いしないことを確認して記録したことです。
