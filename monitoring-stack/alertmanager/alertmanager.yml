global:
  resolve_timeout: 30s

route:
  receiver: lab-webhook
  group_by: [alertname, service]
  group_wait: 1s
  group_interval: 5s
  repeat_interval: 1h

receivers:
  - name: lab-webhook
    webhook_configs:
      - url: http://webhook-receiver:8080/alerts
        send_resolved: true
