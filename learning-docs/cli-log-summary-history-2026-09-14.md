# 集計スクリプトを実際の履歴へ適用

実施日：2026年9月14日（JST）

本人がWSL Ubuntuでsummarize-log.shを、以前の本人操作のHTTP試験で保存したhistory.logへ適用しました。AIが手順を案内し、本人が提示した端末画像を読み取って公開用要約を作成しています。AIの代理実行やCIの実測ではありません。

[人工サンプルでの4通りの判定](./cli-log-summary-script-2026-09-14.md)に続く追加実施です。今回は[本人が保存した実行履歴](./cli-http-history-2026-09-14.md)を読み取りました。新しいHTTP接続やnginxの状態確認は行っていません。

## 本人が実施した操作と結果

| 段階 | 操作 | 提示画面で確認した結果 |
| --- | --- | --- |
| スクリプト検索 | findでsummarize-log.shを検索 | 1件を表示、終了コード0 |
| 履歴検索 | findでhistory.logを検索 | 1件を表示、終了コード0 |
| 集計 | bashでスクリプトを実行し、対象履歴を第1引数に指定 | 全件数3、失敗件数1、判定は失敗記録あり |
| 結果確認 | 実行直後に終了コードを表示 | 集計スクリプトの終了コード1 |
| 記録保存 | 本人が演習要約を保存し、catで再表示 | 保存終了コード0と記録内容を確認 |

実行の形は次のとおりです。summary_scriptとhistory_fileは、事前に確認した各1件のファイルを指します。

```bash
bash "$summary_script" "$history_file"
echo "集計スクリプトの終了コード=$?"
```

結果は[以前の手動集計](./cli-http-log-count-2026-09-14.md)の全3件・失敗1件と一致しました。終了コード1はこのスクリプトが定義した「失敗記録あり」で、読み取りエラーの2とは異なります。

## 過去の失敗と現在の状態を分ける

履歴の失敗1件は、以前に存在しないURLを要求したHTTP 404の記録です。正常URL・存在しないURL・正常URLを手動で試した履歴を集計したものであり、実サービスの障害率や現在発生中の障害とは扱いません。

「この結果だけで今nginxに障害が起きていると判断してよいか」という質問に、本人は「ダメです」と回答しました。AIは、過去の履歴から現在の状態は分からないと補足しました。支援なしの独立再実施は未確認です。

## 保存と証拠の出所

本人がWSL内にlog-summary-history-2026-09-14.txtを保存し、保存終了コード0と内容の再表示を提示しました。既存のhistory.logとスクリプトは変更していません。

- 根拠は会話内の本人提示画像です。個人環境のIP・ユーザー名・ホスト名・絶対パス、鍵情報、スクリーンショット、ローカル原本は公開しません。
- ローカル原本の取得・ハッシュ照合や、第三者の独立再検証は未実施です。
- **NOT RUN**：今回の現在のnginx状態確認、不正形式・権限不足の試験、定期集計、自動通知。
- 所定の正規表現に一致する行を数える既存スクリプトの制約は残っています。欠落・重複・不正形式や集計中のログ変更を検証したわけではありません。
- 静的チェックやCIは文書の検証であり、本人環境での集計の再実行ではありません。

今回の到達点は、人工サンプルで試した集計スクリプトを本人の保存済み履歴へ適用し、以前の手動集計と一致することを確認して記録したことです。
